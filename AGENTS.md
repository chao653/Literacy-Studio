# Literacy-Studio · 项目入口

> 新会话先读本文件，再按「任务路由表」进入对应文档；历史口径先看 `决策日志.md` 顶部 5 条。

## 一、项目范围

- 产品范围：待填（一句话说清 Literacy-Studio 做什么、不做什么）。
- Linear ticket：待填（链接）。

## 二、任务路由表

| 任务类型 | 必读文件 |
| --- | --- |
| 设计系统 / token / 基础组件 | `设计执行/规范/design.md` + `设计执行/规范/components.md` + `设计执行/规范/icons.md` |
| Figma 读写 / 绘制 | `设计执行/规范/figma-preferences.md`（必读，固化 token / 组件来源）+ `设计执行/规范/figma-layer-naming.md` + `需求文档/01_产品规则.md` |
| 卡片类产出（需求说明卡、节点卡） | `设计执行/规范/需求说明与节点卡片规范.md` |
| 英文 UI 文案撰写 / 大小写走查 | `设计执行/规范/写作规范.md` |
| Figma 操作记录落盘 | `设计执行/日志/`（格式见其 `README.md`） |
| 历史决策与口径追溯 | `决策日志.md`（新会话先读顶部 5 条） |
| 多方案探索（HTML demo） | `设计方案/README.md` |
| 设计走查 | `走查/00_走查清单框架.md` |
| 验收报告 | `设计执行/验收/验收报告模板.md` |
| 产品网站取数 / 截图 / 行为验证 | `需求调研/登录说明.md`（用 Playwright MCP；原始产物按抓取需求分类存 `需求调研/抓取结果/<抓取需求>/`，一个需求一个文件夹；基于抓取产出的调研文档归 `需求调研/调研文档/`） |
| Linear 工单 | `Linear/AGENTS.md` |

## 三、决策顺序

1. 用户当前指令
2. 用户指定的节点 / PRD 范围
3. `需求文档/01_产品规则.md` 中已确认的规则
4. 本地执行文档与走查结论
5. 通用判断

冲突若影响业务规则或交付结论，先请用户确认，不自行拍板。

## 四、Figma 工具铁律

- Figma 读写一律用 Figma MCP。
- MCP 不可用时先说明原因，经用户确认后才可换其他途径。
- 在 Figma 完成操作后，把记录写入 `设计执行/日志/`（按类型组文件，格式见其 `README.md`）。
- 走查轮次产物（报告 / 修改清单 / 截图）仍归 `走查/`，与操作日志两边不混。

## 五、输出要求

- 简体中文、大白话，不堆术语。
- 结论区分三类并明确标注：**已确认** / **设计判断** / **需用户确认**。
