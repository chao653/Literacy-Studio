# 登录说明（测试站）

> 安全提醒：
> - 密码只写在本文件，**不写进 `.cursor/rules/`**，不外贴 Notion / Linear。
> - 本文件已登记在项目 `.gitignore`。当前入库的是无账号的占位模板；**填入真实账号前先执行 `git rm --cached 需求调研/登录说明.md`** 让文件脱离 git 跟踪，之后再填写。
>
> 本文写成 agent 可直接执行的格式：用 Playwright MCP 按「登录路径」逐步操作。

## 测试站

- URL：待填
- 默认视口：1440x900

## 角色账号表

| 角色 | 账号 | 密码 | 登录后落点 |
| --- | --- | --- | --- |
| 待填 | 待填 | 待填 | 待填 |

## 登录路径（逐步）

1. 打开 `<测试站 URL>`，视口设为 1440x900。
2. 待填：定位账号输入框（选择器 / 文案），填入账号。
3. 待填：定位密码输入框，填入密码。
4. 待填：点击登录按钮（选择器 / 文案）。
5. 待填：等待落点页面出现（URL 或页面标识元素），确认登录成功。
6. 若有二次验证 / 首次引导弹窗：待填处理方式。

## 产物归档

- 原始产物（截图、HTML、接口返回、录屏）按**抓取需求**分类存 `需求调研/抓取结果/<抓取需求>/`，一个需求一个文件夹。
- 基于抓取产出的调研文档归 `需求调研/调研文档/`。
- 走查用截图归 `走查/截图/`，两边不混。
