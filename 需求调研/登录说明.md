# 登录说明（测试站）

> 安全提醒：
> - 账号密码只写在同目录的 `登录凭据.md`（本地文件，已登记 `.gitignore`，永不入库）；本文件入库，**不得出现任何密码**。
> - 密码不写进 `.cursor/rules/`，不外贴 Notion / Linear。
> - 本仓库是 GitHub 公开仓库，凭据一旦推上去就进入历史记录，收不回。
>
> 本文写成 agent 可直接执行的格式：用 Playwright MCP 按「登录路径」逐步操作。

## 测试站

- URL：<https://staging.kira-learning.com/ai-tools/literacy-studio/>
- 默认视口：1440x900
- 未登录访问会 307 跳到登录页：`https://staging.kira-learning.com/login/?callbackUrl=%2Fai-tools%2Fliteracy-studio%2F`
- 登录页标题 `Log In | Kira`；登录方式为「邮箱 + 密码」两步式（先邮箱、再密码），不是 magic link / 验证码。

## 角色账号表

> 账号与密码填在 `登录凭据.md`（本地）；本表只登记角色与落点。

| 角色 | 账号 | 密码 | 登录后落点 |
| --- | --- | --- | --- |
| 教师（Chao Zhang） | 见 `登录凭据.md` | 见 `登录凭据.md`（区分大小写） | `https://staging.kira-learning.com/ai-tools/literacy-studio/`，Literacy Studio 首页，标题 `Literacy Studio \| Kira` |

## 登录路径（逐步，2026-09-15 已实测全程通过）

1. 打开 `https://staging.kira-learning.com/ai-tools/literacy-studio/`，视口 1440x900；等待跳转到 `/login/?callbackUrl=...`。
2. 登录页标题文案 `Hi! I'm Kira. Welcome! How would you like to continue?`，三个按钮：`Continue with email` / `Continue with Google` / `Continue with other provider`。点击 **`Continue with email`**。
3. 出现 `What's your email address?`，输入框 placeholder `your@email.com`。填入账号（读 `登录凭据.md`），点 **`Next`**（未填邮箱前为 disabled）。
4. 出现 `Welcome back! Enter your password to continue.`，密码框 placeholder `Enter your password`（右侧有显示 / 隐藏密码的眼睛图标；下方有 `Forgot your password?` 链接，不要点）。填入密码（**区分大小写**，照 `登录凭据.md` 原样输入），点 **`Log in`**。
5. 成功：URL 变为 `https://staging.kira-learning.com/ai-tools/literacy-studio/`（callbackUrl 被正确带回），标签页标题 `Literacy Studio | Kira`，主标题 `Good morning, <用户名>`。以「标题含 `Literacy Studio | Kira`」作为登录成功的判定条件。
   失败：密码框下方出现红字 `The email or password you entered is incorrect`。最多重试 1 次；仍失败则停止、不点 `Forgot your password?`，向用户报告（先检查密码大小写）。
6. 二次验证 / 首次引导弹窗：**无**应用内弹窗。有头模式下 Chrome 可能弹浏览器自身的 `Save password?`（按钮 `Never` / `No thanks` / `Save`），点 `No thanks` 即可；Playwright 自动化上下文一般不会出现。
7. 定位方式：页面按钮的 `id` 是 `react-aria-*` 随机值，**不要用 id**；一律按可见文案（`getByRole('button', { name: '...' })`）或 placeholder 定位。

## 登录后页面结构（2026-09-15 实测，供落点校验与导航用）

- 左侧栏：`Home` / `Library`（可展开）/ `Create`（可展开：`AI Tools`、`Coding`、`Survey`、`Assessment`、`Literacy Studio`）/ `Evaluate`（可展开）/ `Classes` / `Student Atlas` / `Class Atlas` / `Manage`（可展开：`Assignments`）；底部 `Notifications`（带未读红点）/ `Help` / `Settings` / 用户名。
- Literacy Studio 首页：副标题 `Your tasks, results, and next steps — all in one place.`；紫色主按钮 `Create Literacy Task`；三张统计卡 `At Risk Students` / `Active Tasks` / `Due This Week`；区块 `Recent Tasks` + 右侧链接 `View All Tasks →`，任务卡带 `Literacy Studio` / `Screener` 标签。
- 产品名在 UI 中写作 `Literacy Studio`（有空格、无连字符）。

## 实测记录

- 2026-09-15：首次提供的密码首字母为小写，两次返回 `The email or password you entered is incorrect`；改为首字母大写后一次通过，落点、标题、侧栏与首页结构如上。无首登引导弹窗。

## 产物归档

- 原始产物（截图、HTML、接口返回、录屏）按**抓取需求**分类存 `需求调研/抓取结果/<抓取需求>/`，一个需求一个文件夹。
- 基于抓取产出的调研文档归 `需求调研/调研文档/`。
- 走查用截图归 `走查/截图/`，两边不混。
