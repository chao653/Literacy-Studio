# 决策日志（Literacy-Studio）

> 全项目拍板记录：产品口径与设计执行的决策都记这里。
>
> 约定：
> - 新决策**置顶**追加（最新的在最上面）。
> - 推翻旧决策时不删原文，新增一条 `[修订]` 条目并指向被推翻的条目。
> - 新会话先读顶部 5 条。
> - 每条格式：`## YYYY-MM-DD · 主题`，正文写 决策 / 依据 / 影响范围 / 状态（已确认 / 设计判断 / 需用户确认）。

---

## 2026-09-24 · 测试站凭据改由 Cursor Secrets 提供给 Cloud Agent

- 决策：Cloud Agent 登录测试站时，账号密码从 Cursor Secrets 的 `KIRA_STAGING_EMAIL` / `KIRA_STAGING_PASSWORD` 读取；本地运行仍用 gitignore 的 `需求调研/登录凭据.md`。仓库文件只写变量名与用法，不写值。
- 依据：用户选择（2026-09-24）；本仓库为 GitHub 公开仓库，密码入库会永久留在历史里；云端 VM 每次重建，本地凭据文件无法跨 run 保留。
- 影响范围：`需求调研/登录说明.md`（凭据来源、Cloud Agent 用法、iframe 定位说明、2026-09-24 实测补充）、`.cursor/rules/literacy-studio.mdc`（测试站一节）。延续 2026-09-15「测试站凭据不入库」一条。
- 状态：已确认。待用户在 Cursor Dashboard 添加两个 Secret 后，新的 run 即可直接登录。

## 2026-09-15 · 图表状态色暂绑 primitives，等语义 chart token

- 决策：Analytics 页图表的 On Track / Monitor / At Risk 三色（#60D16C / #FFC45D / #F9715E）在 Semantic collection 里没有近似 token（最近的 `border/positive` / `border/warning` 色差 47–65），按「找最相近 token」暂绑 primitives `green/400` / `amber/300` / `red/400`；文本颜色一律只在 `text/*` 里取（不用 `text/on-*`、不落到 primitive）。
- 依据：`design.md` §4.1「确实没有语义 token 覆盖的新场景中作为临时依据」；用户指令「找和原参数最相近的 token」。
- 影响范围：`设计执行/日志/Token绑定.md` 2026-09-15 条目涉及的 23 个图表节点。
- 状态：设计判断，需用户确认——是否改用 `fill/positive/default` / `fill/warning/default` / `fill/negative/default`（颜色会明显变深），或请 DS 增加 chart 语义 token。

## 2026-09-15 · 测试站凭据不入库，拆为本地 `需求调研/登录凭据.md`

- 决策：仓库是 GitHub 公开仓库，`需求调研/登录说明.md` 改为只写 URL 与登录路径并入库；账号密码单独放本地 `需求调研/登录凭据.md`，该文件登记 `.gitignore`（替换原先对 `登录说明.md` 的忽略）。
- 依据：脚手架约定「密码不写进 `.cursor/rules/`、不外贴、进 git 的项目要 gitignore」；云端只能靠 git 交付文件，所以把「说明」与「凭据」拆开。
- 影响范围：`需求调研/`、`.gitignore`、`.cursor/rules/literacy-studio.mdc`。
- 状态：设计判断，需用户确认；若认可，建议把这一拆分同步进 `project-scaffold` 技能。

## 2026-09-15 · 项目脚手架初始化

- 决策：按 `project-scaffold` 标准结构创建项目目录与模板文件；`设计执行/规范/` 八份文档整套复制自技能 `references/` 快照（来源 Kira_QuickGrader，2026-08-11 打包）。
- 依据：`project-scaffold` 技能。
- 影响范围：全项目目录结构。
- 状态：已确认（结构）；三库口径、PRD 来源、测试站账号、Linear ticket 待填（见 `AGENTS.md`、`设计执行/规范/figma-preferences.md`、`需求调研/登录说明.md`）。
